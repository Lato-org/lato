<div class="w-100 h-100 d-flex justify-content-center align-items-center" style="min-height: calc(100vh - 54px - 2rem)">
  <div class="card w-100" style="max-width: 450px">
    <div class="card-body">
      <div class="alert alert-warning text-center mb-3">
        <h4 class="alert-heading">Attenzione</h4>
        <p class="mb-0">Sei sicuro di voler uscire dal tuo account?</p>
      </div>
      <div class="d-flex justify-content-between">
        <%= link_to 'Annulla', lato.root_path, class: %w[btn btn-danger] %>
        <%= link_to 'Conferma', lato.authentication_signout_action_path, class: %w[btn btn-success], data: { turbo_method: :delete } %>
      </div>
    </div>
  </div>
</div>